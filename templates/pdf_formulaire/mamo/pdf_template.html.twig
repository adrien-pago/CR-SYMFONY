<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Compte rendu mammographie</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.3;
            margin: 20px;
            font-size: 12pt;
        }
        h1 {
            text-align: center;
            font-size: 14pt;
            font-weight: bold;
            margin-bottom: 15px;
            text-decoration: underline;
        }
        h2 {
            font-size: 13pt;
            margin: 15px 0 5px 0;
        }
        h3 {
            font-size: 12pt;
            margin: 10px 0 5px 0;
        }
        h4 {
            font-size: 11pt;
            margin: 8px 0 4px 0;
        }
        .section {
            margin-top: 15px;
        }
        p {
            margin: 5px 0;
        }
    </style>
</head>
<body>
    <h1>{{ clean_label(titre.titre) }}</h1>

    <div class="section">
        <h2>Indication :</h2>       
        {% if indication.indication == 'Anomalie clinique.' %}
            <p>Anomalie clinique d'une douleur mammaire, latéralité {{indication.masse_lateralite}}</p> 
            <p>apparition {{clean_label(indication.douleur_apparition)}}, ancienneté {{clean_label(indication.douleur_anciennete)}}</p>
            <p>d'une masse mammaire, latéralité  {{indication.masse_lateralite}}, palpation {{clean_label(indication.masse_palpation)}}</p>
            <p>d'un écoulement mammaire, latéralité {{indication.masse_lateralite}}, écoulement {{clean_label(indication.ecoulement_type)}}, {{clean_label(indication.ecoulement_aspect)}}</p>
            

        {% else %}
            <p>{{ indication.indication }}</p>
        {% endif %}
    </div>




    <div class="section">
        <h2>Technique</h2>
        {% if technique is defined and technique.technique is defined and technique.technique %}
            <p>{{ clean_label(technique.technique) }}</p>
        {% endif %}
    </div>

    <div class="section">
        <h2>Comparatif</h2>
        {% if comparatif is defined and comparatif.comparatif is defined and comparatif.comparatif %}
            <p>{{ clean_label(comparatif.comparatif) }}</p>
        {% endif %}
    </div>

    <div class="section">
        <h2>Examen Clinique</h2>
        {% if examen_clinique is defined and examen_clinique.examen_clinique is defined and examen_clinique.examen_clinique %}
            <p>{{ clean_label(examen_clinique.examen_clinique) }}</p>
        {% endif %}
    </div>

    <div class="section">
        <h2>Densité Mammaire</h2>
        <h3>Sein Droit</h3>
        {% if densite_droite is defined and densite_droite.densite_droite is defined and densite_droite.densite_droite %}
            <p>{{ clean_label(densite_droite.densite_droite) }}</p>
        {% endif %}
        {% if densite_echo_droite is defined and densite_echo_droite.densite_echo_droite is defined and densite_echo_droite.densite_echo_droite %}
            <p>Écho : {{ clean_label(densite_echo_droite.densite_echo_droite) }}</p>
        {% endif %}

        <h3>Sein Gauche</h3>
        {% if densite_gauche is defined and densite_gauche.densite_gauche is defined and densite_gauche.densite_gauche %}
            <p>{{ clean_label(densite_gauche.densite_gauche) }}</p>
        {% endif %}
        {% if densite_echo_gauche is defined and densite_echo_gauche.densite_echo_gauche is defined and densite_echo_gauche.densite_echo_gauche %}
            <p>Écho : {{ clean_label(densite_echo_gauche.densite_echo_gauche) }}</p>
        {% endif %}
    </div>
</body>
</html> 